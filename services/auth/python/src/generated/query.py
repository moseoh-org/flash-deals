# Code generated by sqlc. DO NOT EDIT.
# versions:
#   sqlc v1.30.0
# source: query.sql
import dataclasses
import datetime
from typing import Optional
import uuid

import sqlalchemy
import sqlalchemy.ext.asyncio

from generated import models


CREATE_USER = """-- name: create_user \\:one
INSERT INTO auth.users (email, password_hash, name)
VALUES (:p1, :p2, :p3)
RETURNING id, email, name, created_at, updated_at
"""


@dataclasses.dataclass()
class CreateUserRow:
    id: uuid.UUID
    email: str
    name: str
    created_at: datetime.datetime
    updated_at: datetime.datetime


DELETE_USER = """-- name: delete_user \\:exec
DELETE FROM auth.users
WHERE id = :p1
"""


EXISTS_USER_BY_EMAIL = """-- name: exists_user_by_email \\:one
SELECT EXISTS(
    SELECT 1 FROM auth.users WHERE email = :p1
) AS exists
"""


GET_USER_BY_EMAIL = """-- name: get_user_by_email \\:one
SELECT id, email, password_hash, name, created_at, updated_at
FROM auth.users
WHERE email = :p1
"""


GET_USER_BY_ID = """-- name: get_user_by_id \\:one
SELECT id, email, password_hash, name, created_at, updated_at
FROM auth.users
WHERE id = :p1
"""


UPDATE_USER = """-- name: update_user \\:one
UPDATE auth.users
SET name = :p2, updated_at = NOW()
WHERE id = :p1
RETURNING id, email, name, created_at, updated_at
"""


@dataclasses.dataclass()
class UpdateUserRow:
    id: uuid.UUID
    email: str
    name: str
    created_at: datetime.datetime
    updated_at: datetime.datetime


class AsyncQuerier:
    def __init__(self, conn: sqlalchemy.ext.asyncio.AsyncConnection):
        self._conn = conn

    async def create_user(self, *, email: str, password_hash: str, name: str) -> Optional[CreateUserRow]:
        row = (await self._conn.execute(sqlalchemy.text(CREATE_USER), {"p1": email, "p2": password_hash, "p3": name})).first()
        if row is None:
            return None
        return CreateUserRow(
            id=row[0],
            email=row[1],
            name=row[2],
            created_at=row[3],
            updated_at=row[4],
        )

    async def delete_user(self, *, id: uuid.UUID) -> None:
        await self._conn.execute(sqlalchemy.text(DELETE_USER), {"p1": id})

    async def exists_user_by_email(self, *, email: str) -> Optional[bool]:
        row = (await self._conn.execute(sqlalchemy.text(EXISTS_USER_BY_EMAIL), {"p1": email})).first()
        if row is None:
            return None
        return row[0]

    async def get_user_by_email(self, *, email: str) -> Optional[models.AuthUser]:
        row = (await self._conn.execute(sqlalchemy.text(GET_USER_BY_EMAIL), {"p1": email})).first()
        if row is None:
            return None
        return models.AuthUser(
            id=row[0],
            email=row[1],
            password_hash=row[2],
            name=row[3],
            created_at=row[4],
            updated_at=row[5],
        )

    async def get_user_by_id(self, *, id: uuid.UUID) -> Optional[models.AuthUser]:
        row = (await self._conn.execute(sqlalchemy.text(GET_USER_BY_ID), {"p1": id})).first()
        if row is None:
            return None
        return models.AuthUser(
            id=row[0],
            email=row[1],
            password_hash=row[2],
            name=row[3],
            created_at=row[4],
            updated_at=row[5],
        )

    async def update_user(self, *, id: uuid.UUID, name: str) -> Optional[UpdateUserRow]:
        row = (await self._conn.execute(sqlalchemy.text(UPDATE_USER), {"p1": id, "p2": name})).first()
        if row is None:
            return None
        return UpdateUserRow(
            id=row[0],
            email=row[1],
            name=row[2],
            created_at=row[3],
            updated_at=row[4],
        )
