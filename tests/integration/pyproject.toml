[project]
name = "integration"
version = "0.1.0"
description = "API Integration Tests for Flash Deals MSA"
requires-python = ">=3.12"
dependencies = [
    "pytest>=8.3.0",
    "pytest-playwright>=0.5.0",
    "playwright>=1.49.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
